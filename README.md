To compute the behavioral stats used in the paper:

1) Ensure the events TSV files are present in:
   data/sub-*_task-flanker_run-*_events.tsv

2) Run:
   python analysis/compute_stats.py

3) Copy the printed numbers into main.tex (Results + Sample/Exclusions + Open practices access date if needed).

